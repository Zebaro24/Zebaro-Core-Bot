[tool.poetry]
name = "zebaro-core-bot"
version = "0.1.0"
description = "🤖 Multi-platform automation bot: Telegram & Discord bots, FastAPI webhooks, Docker control, GitHub alerts, job scraping."
authors = ["Zebaro <denok100100@gmail.com>"]
readme = "README.md"
package-mode = false

# ---------------------------------------------------------------------------
# Runtime dependencies
# ---------------------------------------------------------------------------
[tool.poetry.dependencies]
python = ">=3.11,<3.15"

# --- Telegram ---
aiogram = "^3.26"

# --- Discord ---
discord-py = "^2.7"

# --- Config / Settings ---
pydantic-settings = "^2.13"

# --- Database ---
pymongo = "^4.16"

# --- Task scheduling ---
apscheduler = "^3.11"

# --- Web scraping ---
playwright = "^1.58"
playwright-stealth = "^2.0"
beautifulsoup4 = "^4.14.3"

# --- HTTP / Webhooks ---
uvicorn = "^0.41"
fastapi = "^0.135"
requests = "^2.32"

# --- Docker management ---
docker = "^7.1"

# --- Network speed testing ---
speedtest-cli = "^2.1.3"

# ---------------------------------------------------------------------------
# Development dependencies
# ---------------------------------------------------------------------------
[tool.poetry.group.dev.dependencies]

# --- Linting & formatting ---
black = "^26.3"
isort = "^8.0"
flake8 = "^7.3"
flake8-bugbear = "^25.11"
flake8-pyproject = "^1.2"

# --- Type checking ---
mypy = "^1.18"

# --- Testing ---
pytest = "^9.0"
pytest-mock = "^3.15"
pytest-cov = "^7.0"
pytest-asyncio = "^1.2"

# --- Security scanning ---
bandit = "^1.9"
safety = "^3.6"

# ===========================================================================
# Tool configuration
# ===========================================================================

# ---------------------------------------------------------------------------
# Black
# ---------------------------------------------------------------------------
[tool.black]
line-length = 120
target-version = ["py311"]
include = '\.pyi?$'
extend-exclude = '''
/(
  \.eggs
  | \.git
  | \.venv
  | build
  | dist
)/
'''

# ---------------------------------------------------------------------------
# isort
# ---------------------------------------------------------------------------
[tool.isort]
profile = "black"
multi_line_output = 3
line_length = 120
known_first_party = ["src"]
sections = ["FUTURE", "STDLIB", "THIRDPARTY", "FIRSTPARTY", "LOCALFOLDER"]

# ---------------------------------------------------------------------------
# Flake8
# ---------------------------------------------------------------------------
[tool.flake8]
max-line-length = 120
extend-ignore = ["E203", "W503"]
exclude = [".git", "__pycache__", ".venv", "venv", "build", "dist", "*.egg-info"]
per-file-ignores = ["__init__.py:F401"]

# ---------------------------------------------------------------------------
# MyPy
# ---------------------------------------------------------------------------
[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_configs = true
disallow_incomplete_defs = true
check_untyped_defs = true
disallow_untyped_decorators = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_no_return = true
warn_unreachable = true
strict_equality = true
show_error_codes = true
disable_error_code = ["no-untyped-def", "import-untyped"]

# ---------------------------------------------------------------------------
# Pytest
# ---------------------------------------------------------------------------
[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = [
    "--verbose",
    "--color=yes",
    "--strict-markers",
    "--strict-config",
    "--rootdir=."
]
filterwarnings = [
    "error",
    "ignore::DeprecationWarning"
]
markers = [
    "asyncio: mark test as asyncio",
    "slow: mark test as slow"
]

# ---------------------------------------------------------------------------
# Coverage
# ---------------------------------------------------------------------------
[tool.coverage.run]
source = ["src"]
omit = ["*/tests/*", "*/test_*", "*/__pycache__/*"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "if self.debug:",
    "if settings.DEBUG",
    "raise AssertionError",
    "raise NotImplementedError",
    "if 0:",
    "if __name__ == .__main__.:",
    "class .*\\bProtocol\\b",
    "@(abc\\.)?abstractmethod"
]

# ---------------------------------------------------------------------------
# Bandit
# ---------------------------------------------------------------------------
[tool.bandit]
exclude_dirs = ["tests", "docs"]
skips = ["B101", "B104"]
