{
  "_": "Скопируй в settings.local.json (он в .gitignore), если нужно переопределить что-то локально. Файл читается Claude Code как конфиг, поэтому НАСТОЯЩИЕ секреты сюда не кладём — их место в .env. Здесь только безобидные локальные настройки.",

  "permissions": {
    "_": "Локальные послабления, если что-то мешает лично тебе. Правила из settings.json они не отменяют — deny всегда сильнее allow.",
    "allow": []
  }
}
